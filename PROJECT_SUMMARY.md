# Project Summary: Personal Brand WB Landing Page

Last updated: 2026-05-20

This file is the persistent working summary for the project. Do not delete information from it when continuing the project. When the project changes, append or update it with the new facts so the site can be continued in a new chat without losing context.

## Update: 2026-05-30 — Final Profit Positioning

The landing page was strategically repositioned from "looking for partners" / generic Wildberries services toward a stronger business value:

`Левон Степанян помогает магазинам Wildberries находить точки потерь, увеличивать чистую прибыль и доводить изменения до измеримого результата.`

Main idea now:

- advertising, SEO, cards, analytics, and sales are not sold as separate isolated services;
- they are tools inside a broader operational profit-management process;
- the main value is finding where the WB store loses money, prioritizing the strongest profit levers, helping implement changes, and controlling the result.

Current header brand:

`Левон Степанян | Рост прибыли на Wildberries`

Current hero headline:

`Помогаю магазинам Wildberries увеличивать чистую прибыль, а не просто обороты`

Current hero subheadline:

`Анализирую экономику бизнеса, рекламу, логистику и операционные процессы. Нахожу точки потерь и помогаю внедрять решения, которые дают измеримый финансовый результат.`

Hero tags:

- `Поиск потерь`
- `Экономика магазина`
- `Оптимизация расходов`
- `Рост чистой прибыли`

Hero buttons:

- primary: `Получить аудит магазина` — scrolls to contact/form block;
- secondary: `Написать в Telegram` — opens `https://t.me/levon_stepanian`.

Compact trust block now contains only:

- `5+ лет` / `на Wildberries`
- `90+ млн ₽` / `заказов за 3 месяца в одном из проектов`
- `От аналитики` / `до внедрения и роста прибыли`

The old visual stats section with:

- `10+ прибыльных проектов`
- `1.000+ протестированных гипотез`
- `100 млн оборот`

was removed from the visible page.

Pain block is now:

Title:

`Рост оборота не всегда означает рост прибыли`

Subtitle:

`Большинство потерь скрыто внутри бизнеса: в рекламе, логистике, упаковке, налогах, экономике товара и операционных процессах.`

Pain cards:

- `Оборот растёт, а чистой прибыли почти нет`
- `Не понимаю, куда уходят деньги`
- `Реклама работает, но маржа падает`
- `Логистика и упаковка съедают прибыль`
- `Есть продажи, но нет системы роста`
- `Боюсь запускать новый товар и потерять деньги`

New diagnostic mini-section added after pain block:

`Где обычно теряются деньги`

Cards:

- `Реклама` — `ДРР растет, а прибыль не увеличивается.`
- `Логистика` — `Товар катается туда-обратно, выкуп низкий, расходы растут.`
- `Упаковка и габариты` — `Незаметные сантиметры превращаются в лишние расходы.`
- `Экономика товара` — `Себестоимость, налоги, комиссии и доставка съедают маржу.`

Main audit block:

Title:

`Аудит и увеличение прибыли магазина Wildberries`

Text:

`Проверяю не только рекламу и карточки. Анализирую всю экономику проекта и нахожу точки потерь, которые мешают бизнесу зарабатывать больше.`

Process:

`Диагностика → Поиск потерь → План действий → Внедрение → Контроль результата`

Checklist:

- `экономику товара и маржинальность`
- `рекламу и ДРР`
- `логистику`
- `упаковку и габариты`
- `налоги и дополнительные расходы`
- `процент выкупа`
- `карточки и конверсию`
- `остатки и склады`

Added explanatory audit note:

`Часто деньги теряются не в одном месте, а сразу в нескольких. Моя задача — найти приоритетные точки роста и показать, какие действия дадут максимальный финансовый эффект.`

Main audit CTA:

`Обсудить аудит`

Directions after audit:

1. `Внедрение и контроль результата`

Text:

`После аудита не оставляю вас с отчетом. Помогаю внедрить изменения, контролировать показатели и доводить решения до измеримого результата.`

2. `Запуск магазина с нуля`

Text:

`Для тех, кто хочет выйти на Wildberries без дорогостоящих ошибок. Помогаю пройти путь от идеи и выбора товара до первых продаж и анализа результатов.`

Launch block bullets:

- `только планируете выход на Wildberries`
- `не хотите терять деньги на типовых ошибках`
- `хотите получить понятный план запуска`
- `нужен опытный наставник на старте`

Education is now a separate full section:

Title:

`Обучение управлению прибыльным магазином Wildberries`

Text:

`Обучение строится на реальных проектах и цифрах. Без теории ради теории. Показываю, как работать с аналитикой, рекламой, карточками, экономикой и управлением магазином.`

Suitable for:

- `начинающим менеджерам Wildberries`
- `действующим менеджерам`
- `начинающим селлерам`
- `владельцам магазинов`

Topics:

- `аналитика`
- `реклама`
- `SEO и карточки`
- `увеличение продаж`
- `экономика магазина`
- `поиск точек роста`
- `операционное управление`

Results/cases block preserved. Current semantic emphasis:

`Комплексная работа: продажи, расходы, экономика и чистая прибыль.`

Existing case remains with screenshots and lightbox. Placeholder for second case remains:

- `От запуска до 6,5 млн ₽ за месяц`
- `Кейс в подготовке — добавлю после закрытия месяца.`

Partnership block was visually separated as premium/closed format:

Kicker:

`Партнерство`

Title:

`Рассматриваю ограниченное количество проектов для долгосрочного сотрудничества`

Text:

`В отдельных проектах могу участвовать не только как консультант, а как операционный партнер: помогать внедрять решения, выстраивать процессы и участвовать в развитии бизнеса.`

Subtitle:

`Это формат не для разовой консультации, а для проектов, где есть потенциал системного роста.`

Suitable if:

- `действующий магазин на Wildberries`
- `собственное производство`
- `крупный опт или поставки`
- `товарная линейка с потенциалом масштабирования`
- `желание строить системный бизнес, а не искать разовые решения`

CTA:

`Обсудить партнерство`

Form dropdown options now:

- `Аудит магазина`
- `Внедрение и сопровождение`
- `Запуск магазина с нуля`
- `Обучение и наставничество`
- `Партнерство`

Telegram notification now starts with:

`🎯 Тип обращения:`

and still includes:

- name;
- contact;
- project;
- current page;
- UTM source, medium, campaign, and content;
- missing UTM values as `не передан`.

Current contact links:

- Telegram: `https://t.me/levon_stepanian`
- WhatsApp: `https://wa.me/79283222448`
- MAX: `https://max.ru/u/f9LHodD0cOImftilzkCl4kAeStuy19clTRT-OfiR8N3evEhlv8ZSIie6VrY`
- Instagram: `https://www.instagram.com/spartanec_wb?igsh=ZTFmcDhta284ZXRy&utm_source=qr`
- Phone: `tel:+79283222448`

Yandex Metrica goals preserved:

- `telegram_click`
- `whatsapp_click`
- `max_click`
- `instagram_click`
- `form_submit`

Files changed in this update:

- `index.html` — final profit positioning, new texts, new diagnostic section, updated service/audit/education/partnership/form content, updated OG site name.
- `styles.css` — visual styling for hero tags, diagnostic leak cards, audit note, launch bullets, education section, and premium partnership block; responsive behavior for new blocks.
- `api/telegram.js` — Telegram notification label changed to `Тип обращения`.
- `package.json` — project description updated to reflect the profit-consulting positioning.
- `PROJECT_SUMMARY.md` — this detailed update section.

Verification performed in this update:

- `node --check script.js` passed.
- `node --check api/telegram.js` passed.
- `node --check server.js` passed.
- static asset path check found no missing referenced local files.
- direct Node build command passed: `Static landing is ready for Vercel`.
- Telegram handler mock test passed and produced the expected UTM-aware notification with `Тип обращения`.
- in-app browser check confirmed current hero/header texts, 4 hero tags, 3 trust cards, 6 pain cards, 4 diagnostic cards, 8 audit checklist items, education section, 7 education tags, no visible old stats panel, no mailto contact links, and no horizontal overflow at desktop width.
- result screenshot lightbox opens, shows the original screenshot, and next-month navigation works.

Known environment note:

`npm run build` could not be executed in the current shell because `npm` was not available in PATH. The underlying build command from `package.json` was executed directly with `node` and passed. On Vercel/GitHub, npm should be available normally.

## Update: 2026-05-31 — Concrete Service Block Visual System

The user requested that vague instructions such as "make it premium" or "show as cards" should not be used without a concrete visual pattern. The site was updated so every service block has a clearly defined layout and visual logic.

### Audit Block

The audit section is still the main offer:

`Аудит и увеличение прибыли магазина Wildberries`

The audit block now contains four concrete parts:

1. `Кому подходит`

Implemented as a pure checklist, not cards.

Visual rule:

- each item is a horizontal row;
- red check icon `✔` in color `#C41E3A`;
- text on the right;
- no card backgrounds around individual items.

Checklist items:

- `Оборот растет, а прибыль нет`
- `Много рекламы, но денег остается мало`
- `Не понимаете, где теряется маржа`
- `Хотите масштабироваться без потери прибыли`
- `Продажи есть, но бизнес работает нестабильно`

2. `Что входит в аудит`

Replaced old small capsule list with large cards.

Desktop:

- 2 columns.

Mobile/tablet:

- 1 column.

Cards:

- `📊 Экономика и прибыль` — `Проверяю маржинальность, себестоимость, unit-экономику и чистую прибыль.`
- `📢 Реклама` — `Анализирую ДРР, рекламные расходы и эффективность рекламных кампаний.`
- `🚚 Логистика` — `Проверяю склады, остатки, упаковку и влияние логистики на прибыль.`
- `📈 Карточки товаров` — `Анализирую CTR, конверсию, процент выкупа и точки роста.`
- `⚙ Операционные процессы` — `Проверяю дополнительные расходы, налоги и внутренние процессы.`

3. `Как проходит работа`

This is now part of the audit section, not a separate page section.

Desktop:

- horizontal roadmap: `01 → 02 → 03 → 04 → 05`;
- thin red line behind steps;
- each step has number, title, and one sentence.

Mobile/tablet:

- vertical roadmap;
- red vertical line;
- steps stacked as `01 ↓ 02 ↓ 03 ↓ 04 ↓ 05`.

Audit steps:

- `01 Диагностика` — `Собираем исходные цифры магазина и фиксируем текущую картину.`
- `02 Поиск потерь` — `Нахожу, где реклама, логистика, налоги и процессы съедают прибыль.`
- `03 План действий` — `Расставляю приоритеты и показываю, что даст максимальный эффект.`
- `04 Внедрение` — `Помогаю внедрить решения или сопровождаю вашу команду.`
- `05 Контроль результата` — `Смотрим динамику прибыли, расходов и масштабируем рабочие решения.`

4. `После аудита вы получаете`

Implemented as result cards with light green background.

Cards:

- `список точек потерь`
- `план действий`
- `финансовые ориентиры`
- `рекомендации по увеличению прибыли`

### Launch From Zero Block

The previous "Запуск магазина с нуля" card was turned into a separate full section with the same visual logic as the audit block.

Section ID:

`#launch`

Title:

`Запуск магазина Wildberries с нуля`

Text:

`Для тех, кто хочет выйти на Wildberries без дорогостоящих ошибок. Помогаю пройти путь от идеи и выбора товара до первых продаж и анализа результатов.`

Structure:

1. `Кому подходит` — checklist.
2. `Что входит` — large cards.
3. `Как проходит работа` — roadmap.
4. `Что получает клиент` — green result cards.

Launch checklist:

- `Только планируете выход на Wildberries`
- `Не хотите терять деньги на типовых ошибках`
- `Хотите заранее понять экономику запуска`
- `Нужен понятный план первых действий`
- `Нужен опытный наставник на старте`

Launch cards:

- `📦 Товар и ниша`
- `📊 Экономика запуска`
- `🧾 Карточка товара`
- `📢 Первые продажи`

Launch roadmap:

- `01 Разбор идеи`
- `02 Расчет экономики`
- `03 Подготовка запуска`
- `04 Старт продаж`
- `05 Анализ результата`

Launch result cards:

- `план запуска`
- `расчет экономики`
- `структуру карточки`
- `первые гипотезы роста`

### Education Block

Education remains a separate section but now follows the same structured pattern.

Title:

`Обучение управлению прибыльным магазином Wildberries`

Structure:

1. `Кому подходит` — checklist.
2. `Что изучаем` — large cards.
3. `Как проходит обучение` — roadmap.
4. `Что получает клиент` — green result cards.

Study cards:

- `📊 Аналитика`
- `📢 Реклама`
- `📦 Экономика`
- `🔍 SEO`
- `📈 Управление прибылью`
- `🚀 Масштабирование`

Education result cards:

- `понимание экономики WB`
- `навык работы с аналитикой`
- `систему принятия решений`
- `план развития навыков`

### Separate "How Work Goes" Section Removed

The old standalone section:

`#approach`

was removed because the user requested "Как проходит работа" to be part of the audit block rather than a separate site section.

Header navigation was updated:

- `Аудит` → `#services`
- `Запуск` → `#launch`
- `Обучение` → `#education`
- `Результаты` → `#results`
- `Партнерство` → `#partnership`
- `Контакты` → `#contact`

### Partnership Block Upgrade

The partnership block was made the most visually distinct section.

Visual implementation:

- dark graphite background;
- soft golden radial glow at the edges;
- very slow background animation;
- separate premium card with gold border;
- hover state slightly increases glow;
- no neon or acidic effects.

Scarcity/accent element added:

`✦ Рассматриваю ограниченное количество проектов для долгосрочного сотрудничества.`

This appears as a separate larger accent element before the main heading.

Partnership heading changed to:

`Формат операционного партнерства для проектов с потенциалом роста`

Partnership button changed to:

`Подать заявку на партнерство`

Button visual:

- gold gradient from dark gold to light gold;
- subtle left-to-right shine on hover;
- no aggressive animation.

### Files changed in this update

- `index.html` — restructured audit, launch, education, and partnership sections; removed separate `#approach` section; updated navigation.
- `styles.css` — added concrete visual systems for checklists, 2-column cards, roadmaps, green result cards, launch/education sections, and premium animated partnership block.
- `PROJECT_SUMMARY.md` — this update entry.

### Verification performed

- `node --check script.js` passed.
- `node --check api/telegram.js` passed.
- `node --check server.js` passed.
- local asset path check found no missing referenced files.
- direct Node build command passed: `Static landing is ready for Vercel`.
- Telegram handler mock still returns success and preserves `Тип обращения`.
- browser DOM check confirmed:
  - audit checklist has 5 items;
  - audit cards count is 5;
  - audit roadmap has 5 steps;
  - audit result cards count is 4;
  - launch section exists;
  - launch checklist has 5 items;
  - launch cards count is 4;
  - launch roadmap has 5 steps;
  - launch result cards count is 4;
  - education study cards count is 6;
  - education roadmap has 5 steps;
  - old `#approach` section is removed;
  - partnership button text is `Подать заявку на партнерство`;
  - no horizontal overflow was detected in the in-app browser check.

## Project Location

Workspace:

`/Users/levonstepanian/Documents/Codex/2026-05-15/wildberries`

Main local page:

`/Users/levonstepanian/Documents/Codex/2026-05-15/wildberries/index.html`

The user often opens the site directly in the in-app browser through:

`file:///Users/levonstepanian/Documents/Codex/2026-05-15/wildberries/index.html`

For verification, direct `file://` navigation may be blocked by browser security policy. In that case, run the local static server on `127.0.0.1:8080` from the project root, verify through `http://127.0.0.1:8080/`, then stop the server.

Static server command used previously:

```bash
node -e "const http=require('http'),fs=require('fs'),path=require('path');const root=process.cwd();const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg'};http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');let p=path.join(root,url.pathname==='/'?'index.html':decodeURIComponent(url.pathname));if(!p.startsWith(root)){res.writeHead(403);res.end('Forbidden');return;}fs.readFile(p,(err,data)=>{if(err){res.writeHead(404);res.end('Not found');return;}res.writeHead(200,{'Content-Type':types[path.extname(p)]||'application/octet-stream'});res.end(data);});}).listen(8080,'127.0.0.1',()=>console.log('http://127.0.0.1:8080'));"
```

Always stop the local server after verification:

```bash
lsof -ti tcp:8080
kill <pid>
```

## Project Goal

Create a premium one-page personal brand website for Levon Stepanian, positioned as an operational Wildberries partner.

The site should not feel like a generic marketplace manager service page. It should feel like a premium personal brand and operational growth partner for Wildberries businesses.

Core message:

Levon does not just "manage a WB cabinet"; he enters strong product projects as an operational partner and helps build controlled business growth through analytics, economics, advertising, product cards, hypotheses, and daily growth decisions.

## Positioning

Name:

`Левон Степанян`

Header brand label:

`Операционный партнер WB`

Primary positioning:

`Операционный партнер Wildberries`

Main hero headline:

`Захожу в сильные товарные проекты и довожу магазин WB до управляемого роста`

Important positioning nuance:

The user wants to work in partnership format with projects where there is already a base for growth. This can be an active seller, a person/company with production, a major wholesaler, a brand representative, a manufacturer, or someone with a product line ready for marketplace development.

Do not write bluntly that Levon does not select products. Instead, phrase it softly around working with projects that already have a product base, brand, production, wholesale, an active store, or a product line that can be systematically developed on Wildberries.

## Target Audience

The site is for:

- active Wildberries sellers;
- WB store owners;
- manufacturers;
- major wholesalers;
- brand representatives;
- owners of product lines;
- entrepreneurs who want to enter Wildberries with an existing product/product base;
- businesses that need systematic marketplace growth rather than one-time cabinet management.

Priority audience:

Projects where the store already works or the product is already available and ready to scale.

## Design Direction

Desired style:

- premium personal brand;
- operational partner / investment-grade business site;
- Apple x Stripe x investment fund;
- dark, expensive, minimalist;
- glassmorphism used carefully;
- restrained gold accents;
- large, confident typography;
- smooth animations;
- not a stock corporate template;
- not a generic agency service page;
- not over-decorated;
- no random decorative blobs or one-note palette;
- user should understand in a few seconds: this is business growth management, not just cabinet administration.

Design constraints already accepted:

- keep the dark premium style;
- keep the current site structure;
- keep the current animation logic;
- avoid changing successful concept unless explicitly requested;
- only make requested changes when user asks for focused updates.

## Current Site Structure

The page is a static one-page HTML/CSS/JS site.

Sections:

1. Header/navigation
2. Hero
3. Key metrics panel
4. "Кому полезно"
5. "Что беру на себя в проекте"
6. "Подход"
7. "Результаты в цифрах"
8. Contact block
9. Footer
10. Lightbox modal for result screenshots

## File List And Purpose

`index.html`

- main page markup;
- all visible content/text;
- all section structure;
- result slider markup;
- result screenshot references;
- contact links and form;
- lightbox modal markup.

`styles.css`

- full visual design;
- responsive layout;
- dark premium styling;
- buttons, cards, grids, slider visuals;
- screenshot thumbnail layout;
- lightbox styling;
- mobile adaptation.

`script.js`

- header scroll state;
- animated counters in stats section;
- animated counters in result slides;
- result slider behavior;
- lightbox behavior for screenshots;
- keyboard support for screenshot opening and lightbox navigation.

`assets/wb-sales-hero.png`

- hero background image.

`assets/levon-portrait.png`

- main portrait photo of Levon used in the hero portrait card;
- copied from `/Users/levonstepanian/Downloads/Frame 1 (23).png`;
- original dimensions: 1800 x 2400;
- important user requirements: do not alter face or appearance, do not create AI/CGI/cartoon effect, preserve natural realistic look, use only light integration through CSS such as fit, crop, contrast, color, and positioning.

Result funnel screenshots:

- `assets/result-funnel-nov.png` — November 2025 funnel screenshot, "БЫЛО".
- `assets/result-funnel-dec.png` — December 2025 funnel screenshot, "БЫЛО".
- `assets/result-funnel-jan.png` — January 2026 funnel screenshot, "СТАЛО".
- `assets/result-funnel-feb.png` — February 2026 funnel screenshot, "СТАЛО".
- `assets/result-funnel-mar.png` — March 2026 funnel screenshot, "СТАЛО".

Result metric screenshots:

- `assets/result-metrics-nov.png` — November 2025 metrics screenshot, "БЫЛО".
- `assets/result-metrics-dec.png` — December 2025 metrics screenshot, "БЫЛО".
- `assets/result-metrics-after.png` — January 2026 metrics screenshot, "СТАЛО".
- `assets/result-metrics-feb.png` — February 2026 metrics screenshot, "СТАЛО".
- `assets/result-metrics-mar.png` — March 2026 metrics screenshot, "СТАЛО".

Important: On 2026-05-20 all result screenshots were replaced with newer versions where the selected date range is visible at the top. This is important for trust and proof.

New screenshots used:

Funnel screenshots:

- November 2025: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.39.31.png` copied to `assets/result-funnel-nov.png`
- December 2025: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.39.43.png` copied to `assets/result-funnel-dec.png`
- January 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.40.00.png` copied to `assets/result-funnel-jan.png`
- February 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.40.15.png` copied to `assets/result-funnel-feb.png`
- March 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.40.36.png` copied to `assets/result-funnel-mar.png`

Metrics screenshots:

- November 2025: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.41.18.png` copied to `assets/result-metrics-nov.png`
- December 2025: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.41.29.png` copied to `assets/result-metrics-dec.png`
- January 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.41.41.png` copied to `assets/result-metrics-after.png`
- February 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.41.52.png` copied to `assets/result-metrics-feb.png`
- March 2026: `/Users/levonstepanian/Desktop/Снимок экрана 2026-05-19 в 20.42.02.png` copied to `assets/result-metrics-mar.png`

Current asset dimensions after replacement:

- `result-funnel-nov.png`: 2880 x 1402
- `result-funnel-dec.png`: 2904 x 1414
- `result-funnel-jan.png`: 2904 x 1412
- `result-funnel-feb.png`: 2924 x 1416
- `result-funnel-mar.png`: 2918 x 1404
- `result-metrics-nov.png`: 2874 x 1100
- `result-metrics-dec.png`: 2864 x 1082
- `result-metrics-after.png`: 2876 x 1100
- `result-metrics-feb.png`: 2870 x 1110
- `result-metrics-mar.png`: 2870 x 1104

## Contact Details And Links

Telegram:

- display: `@levon_stepanian`
- URL: `https://t.me/levon_stepanian`
- opens in new tab.

Email:

- current correct email: `stepanianlevon022@mail.ru`
- mailto: `mailto:stepanianlevon022@mail.ru`

Previous incorrect email that was fixed:

- `stepaninalvon022@mail.ru`

Phone:

- display: `+7 (928) 322-24-48`
- tel link: `tel:+79283222448`

CTA buttons:

- header CTA text: `Обсудить партнёрство`
- hero primary CTA text: `Обсудить партнёрство`
- both link to `https://t.me/levon_stepanian` and open in a new tab.

Contact form:

- uses `mailto:stepanianlevon022@mail.ru`;
- note says the form opens the mail app on the computer.

## All Main Texts Currently Used

Title:

`Левон Степанян | Операционный партнер WB`

Meta description:

`Операционное партнерство для действующих селлеров, производителей, оптовиков и брендов, которые хотят развивать продажи на Wildberries.`

Header:

- brand: `Операционный партнер WB`
- nav: `Партнерство`, `Подход`, `Результаты`, `Контакты`
- CTA: `Обсудить партнёрство`

Hero:

Eyebrow:

`Левон Степанян • операционный партнер Wildberries`

Headline:

`Захожу в сильные товарные проекты и довожу магазин WB до управляемого роста`

Text:

`Партнерство для селлеров, производителей, оптовиков и брендов, у которых уже есть продуктовая база. Я беру на себя операционное развитие: экономику, карточки, рекламу, аналитику, гипотезы и ежедневные решения по росту.`

Buttons:

- `Обсудить партнёрство`
- `Смотреть показатели`

Proof tags:

- `Стратегия`
- `Операционка`
- `Рост продаж`

Portrait card:

- `Левон Степанян`
- `Партнер по росту магазина на WB`
- Uses real portrait photo from `assets/levon-portrait.png`.

Stats panel:

Kicker:

`Показатели`

Headline:

`Работаю на стороне результата, а не красивых отчетов.`

Stats:

- `10+ прибыльных проектов`
- `1.000+ протестированных гипотез`
- `100 млн оборот`

"Кому полезно":

Kicker:

`Кому полезно`

Text:

`Это может быть действующий магазин, собственное производство, крупный опт, бренд или товарная линейка, которую можно системно вывести и развивать на маркетплейсе.`

Services:

Kicker:

`Партнерство`

Headline:

`Что беру на себя в проекте`

Intro text:

`Формат подходит, когда нужен не внешний советчик, а человек, который включается в развитие магазина и отвечает за движение по цифрам.`

Service cards:

1. `Диагностика потенциала`

`Разбираю товар, нишу, текущие продажи, конкурентов, маржинальность и точки роста, чтобы понять, где проект может прибавить быстрее всего.`

2. `Стратегия развития`

`Формирую план по ассортименту, ценам, карточкам, рекламе, акциям и приоритетам, чтобы магазин развивался не хаотично, а по понятной системе.`

3. `Продвижение и карточки`

`Улучшаю видимость товара, упаковку карточек и рекламные кампании, чтобы трафик приводил к заказам, а бюджет работал на рост.`

4. `Управление ростом`

`Держу в фокусе аналитику, остатки, акции, поставки, динамику продаж и регулярные решения, от которых зависит результат магазина.`

Approach:

Kicker:

`Подход`

Headline:

`Работаю как партнер, а не как разовый подрядчик`

Text:

`Сначала смотрю потенциал проекта, затем выстраиваю систему действий и регулярно сверяю решения с фактическими показателями.`

Steps:

1. `Знакомимся с проектом`

`Смотрим товар, нишу, маржинальность, складские возможности, текущий магазин или готовность к запуску.`

2. `Договариваемся о формате`

`Определяем зону ответственности, цели, экономику партнерства и первые приоритеты в развитии.`

3. `Развиваем магазин`

`Запускаем изменения, отслеживаем динамику, усиливаем рабочие гипотезы и убираем лишние действия.`

Results:

Kicker:

`Результаты в цифрах`

Headline:

`Это не ведение кабинета. Это управление ростом бизнеса.`

Text:

`Один проект до и после входа в партнерство: ноябрь-декабрь как база, январь-февраль-март как период системного роста.`

Tags:

- `Масштабировали`
- `Оптимизировали`
- `Сократили расходы`
- `Увеличили прибыль`
- `Перестроили экономику`
- `Системный рост`
- `Работа через цифры`

Contact:

Kicker:

`Контакты`

Headline:

`Давайте обсудим, есть ли у проекта потенциал для партнерства`

Text:

`Напишите, какой у вас товар, бренд, производство или действующий магазин, и на какой стадии сейчас проект. Я посмотрю, как можно зайти в развитие продаж на Wildberries.`

Form labels:

- `Имя`
- `Контакт`
- `Проект`

Form button:

`Отправить проект`

Form note:

`Форма откроет почтовую программу на вашем компьютере.`

Footer:

- `Левон Степанян`
- `Партнерство для роста на Wildberries`

## Results Block Logic

The "Результаты в цифрах" section is one of the most important proof blocks.

Core logic:

`Было -> Что сделали -> Стало`

Top labels:

- `Было`: `Ноябрь-декабрь`
- `Что сделали`: `Экономика, реклама, карточки, гипотезы`
- `Стало`: `Январь-февраль-март`

Meaning:

- `Было` is the state of the project before Levon entered the partnership.
- `Стало` is the state after Levon entered and the project began systematic operational work.

Period before partnership:

- November 2025
- December 2025

Period after entry:

- January 2026
- February 2026
- March 2026

Current screenshot display logic:

- No spotlight.
- No frames.
- No circles.
- No automatic highlighting.
- No arrows on screenshots.
- Clean thumbnails only.
- Each thumbnail has:
  - month/status label: `Ноябрь — БЫЛО`, `Декабрь — БЫЛО`, `Январь — СТАЛО`, `Февраль — СТАЛО`, `Март — СТАЛО`;
  - small metric label: `Заказы`, `Чистая прибыль`, `ROI`, `Реклама`, `Прибыль на 1 шт`, `Расходы`.
- Clicking a screenshot opens it in a premium Apple-style lightbox modal.
- In lightbox, the original screenshot is shown without modifications.
- No automatic highlights in lightbox.
- Dates must remain visible.
- Interface must not be cropped.
- Lightbox has previous/next month navigation.
- If January is opened, arrows can navigate to February and March.
- Keyboard support exists for Enter/Space on thumbnails, Escape to close, ArrowLeft/ArrowRight to navigate.

## Results Slides And Metrics

There are 6 slides in the result slider.

### Slide 1: Рост заказов

Eyebrow:

`Рост проекта после входа в партнерство`

Title:

`Рост заказов`

Big number:

`×4.4`

Range:

`5 301 шт → 22 075 шт`

Text:

`Масштабировали продажи и выстроили систему роста.`

Insights:

- `Заказы выросли не за счет одного всплеска, а через системную динамику.`
- `На каждом месяце видна воронка: от переходов до выкупов.`

Screenshots:

- Uses funnel screenshots for November, December, January, February, March.

### Slide 2: Рост чистой прибыли

Big number:

`×18`

Range:

`196 тыс ₽ → 3.51 млн ₽`

Text:

`Перестроили экономику проекта и кратно увеличили прибыль.`

Insights:

- `Фокус сместился с оборота ради оборота на прибыльность.`
- `Проект начал зарабатывать больше с каждой операционной связки.`

Screenshots:

- Uses metrics screenshots for November, December, January, February, March.

### Slide 3: ROI

Big number:

`×3.3`

Range:

`26.7% → 88.4%`

Text:

`Увеличили отдачу на вложенные деньги.`

Insights:

- `Деньги начали возвращаться быстрее и эффективнее.`
- `Рост стал опираться на экономику, а не на давление бюджетом.`

Screenshots:

- Uses metrics screenshots for November, December, January, February, March.

### Slide 4: Оптимизация рекламы

Big number:

`×16`

Range:

`4.45% → 0.27%`

Text:

`Снизили рекламную нагрузку и убрали лишние расходы.`

Insights:

- `Реклама перестала съедать экономику проекта.`
- `Ставка сделана на точечные гипотезы и контроль ДРР.`

Screenshots:

- Uses metrics screenshots for November, December, January, February, March.

### Slide 5: Прибыль на единицу

Big number:

`×5.5`

Range:

`57 ₽ → 311 ₽`

Text:

`Увеличили эффективность каждой продажи.`

Insights:

- `Каждая проданная единица стала приносить больше денег.`
- `Рост прибыли поддержан ассортиментом, ценами и расходами.`

Screenshots:

- Uses metrics screenshots for November, December, January, February, March.

### Slide 6: Оптимизация расходов

Big number:

`−50%`

Range:

`комиссия, хранение, реклама и ДРР под контролем`

Text:

`Убрали лишнюю нагрузку и сделали экономику проекта прозрачной.`

Insights:

- `Расходы стали управляемыми, а не “фоном” в отчете.`
- `Каждый рубль проверяется через влияние на маржинальность и ROI.`

Screenshots:

- Uses metrics screenshots for November, December, January, February, March.

## Main Result Numbers

Important numbers requested by user:

- growth in turnover / orders: `×4.4`
- growth in net profit: `×18`
- ROI growth: `×3.3`
- ad optimization: `×16`
- profit per unit growth: `×5.5`

Additional current slide:

- expense optimization: `−50%`

## Accepted User Requirements And Constraints

General:

- User is not a programmer; explain simply when explaining is needed.
- User wants the site as ready-to-use as possible.
- Work proactively and implement, not just propose.
- Keep the site premium, alive, and non-template.
- Avoid making it feel like "services of a manager".
- The site should convey partnership and business growth management.

Design:

- Premium dark style.
- Apple x Stripe x investment fund feeling.
- Minimalist expensive style.
- Smooth animations.
- Glass effects are allowed if tasteful.
- No stock corporate template feeling.
- No overloading the screenshots with highlights.
- Do not change current design unless specifically requested.
- Do not change current site structure unless specifically requested.
- Do not break current animations.

Result screenshots:

- Use new screenshots where top date ranges are visible.
- Dates must be visible for trust and proof.
- Show original screenshots in lightbox without modifications.
- Do not crop interface.
- Do not use automatic highlights, spotlights, frames, circles, or arrows on screenshots.
- Thumbnails can have only month/status and small metric label.
- Screenshots should open in large modal on click.
- Lightbox should have background dimming and Apple-style animation.
- Lightbox should allow month navigation.

Contacts:

- Telegram must be clickable and open in new tab.
- Email must be clickable through `mailto:`.
- Phone must be clickable through `tel:`.
- Current correct email is `stepanianlevon022@mail.ru`.

Project Summary:

- User asked to create and update Project Summary before context compression or session ending.
- Store project structure, decisions, links, contacts, design logic, completed work, remaining tasks, prompts/rules, latest changes, file list, user requirements, and constraints.
- Do not delete information from Project Summary.
- If context gets compressed automatically, preserve a detailed working summary so a new chat can continue without context loss.
- This file is the persistent Project Summary.

## Implemented Changes So Far

Initial site:

- Created one-page website for the personal brand.
- Added premium hero.
- Added name `Левон Степанян`.
- Added header brand `Операционный партнер WB`.
- Added operational partner positioning.

Positioning refinement:

- Shifted from "WB manager services" to "operational partnership".
- Added audience around sellers, manufacturers, wholesalers, brands, and product lines.
- Avoided wording that directly says Levon does not do product selection.

Premium redesign:

- Improved hero.
- Added portrait area for photo.
- Added big numbers block:
  - `10+ прибыльных проектов`
  - `1.000+ протестированных гипотез`
  - `100 млн оборот`
- Improved buttons and premium dark feel.

Results block:

- Created interactive "Результаты в цифрах" block.
- Built slider with `Было -> Что сделали -> Стало`.
- Added 6 result slides.
- Added screenshot grid for each slide.
- Added counters and slide animations.
- Added real WB analytics screenshots.
- Initially tried spotlight highlights, then removed them after user feedback.
- Current result screenshot system is clean thumbnails + lightbox.

Lightbox:

- Added modal with dark dimmed background.
- Added smooth opening animation.
- Added large original screenshot view.
- Added previous/next month buttons.
- Added keyboard navigation.

Contacts:

- Added Telegram, email, phone.
- Made Telegram, mail, phone clickable.
- Updated email to `stepanianlevon022@mail.ru`.

Screenshots:

- Replaced old screenshots with new versions where date ranges are visible.
- Kept same file names so site structure and HTML references remain stable.

Portrait photo:

- Added the user's real photo to the hero portrait/information card.
- Original file: `/Users/levonstepanian/Downloads/Frame 1 (23).png`.
- Site asset: `assets/levon-portrait.png`.
- No AI generation, no face modification, no cartoon/CGI effect.
- The photo is integrated with CSS using `object-fit: cover`, `object-position: 68% 43%`, and light visual tuning through `filter: saturate(0.96) contrast(1.04) brightness(0.98)`.
- The old `ЛС` initials placeholder was removed from the HTML.

Verification already performed:

- `node --check script.js` passed after lightbox work.
- Browser verification for lightbox passed:
  - no old highlight classes;
  - no spotlight pseudo-elements;
  - click opens modal;
  - next month works;
  - January can navigate to February and March;
  - no browser errors.
- Browser verification after screenshot replacement:
  - all 10 updated screenshot assets loaded;
  - dimensions matched new large screenshots;
  - correct email present;
  - old email not present;
  - no browser errors.
- Browser verification after portrait insertion:
  - `.portrait-photo` exists;
  - source is `assets/levon-portrait.png`;
  - image loads successfully;
  - natural dimensions are 1800 x 2400;
  - old `.portrait-placeholder` is not present;
  - object fit is `cover`;
  - object position is `68% 43%`;
  - no browser errors.

## Remaining Tasks / Potential Next Steps

Still useful to do later:

- Check final mobile layout carefully after all screenshot replacements.
- Check final mobile layout carefully after the real portrait insertion.
- Consider reducing thumbnail heights if new screenshots with date headers make the result grid visually too tall.
- Add favicon.
- Add Open Graph/social preview image for Telegram sharing.
- Connect form to a better system than `mailto`, if needed:
  - Telegram bot;
  - Tally;
  - Google Forms;
  - Make;
  - CRM.
- Add privacy policy if collecting personal data through a public form.
- Publish to hosting/domain.
- Add analytics such as Yandex Metrica or Google Analytics.
- Potentially add "Формат партнерства" block if the user wants to filter applicants more strongly.
- Keep updating this `PROJECT_SUMMARY.md` after important project changes.

## Current Important Implementation Notes

- The current site is static. No build step is required.
- Opening `index.html` directly works.
- The hero portrait card now uses `assets/levon-portrait.png` through `.portrait-photo`.
- The photo is not edited as a bitmap; the natural image is preserved and only styled in CSS for fit and integration.
- A local server is useful for browser verification, especially because direct browser automation of `file://` may be blocked.
- `script.js` is loaded as `script.js?v=5`.
- The results slider uses `data-slider`, `data-case-track`, `data-case-slide`, `data-case-dot`, `data-case-prev`, and `data-case-next`.
- The lightbox uses:
  - `[data-lightbox]`
  - `[data-lightbox-image]`
  - `[data-lightbox-month]`
  - `[data-lightbox-metric]`
  - `[data-lightbox-close]`
  - `[data-lightbox-prev]`
  - `[data-lightbox-next]`
- Screenshot thumbnails use `.evidence-shot`, `.month-label`, and `.metric-mark`.
- Current `.metric-mark` is only a small label, not a highlight.
- There should be no `highlight-*` classes in current HTML.
- There should be no old email `stepaninalvon022@mail.ru` in current HTML.

## Working Rule For Future Codex Sessions

Before ending a major work session or when context may be compacted:

1. Open this file.
2. Add a "Last updated" note if relevant.
3. Append latest changes.
4. Preserve old decisions and constraints.
5. Do not replace detailed context with a short summary.
6. Keep enough detail for a new chat to continue the project without asking the user to repeat requirements.

## Launch Preparation Update: 2026-05-20

The site was prepared as a working advertising landing page for traffic from Telegram, Yandex Direct, and other ad platforms.

Project name:

`levonwb-partner`

Desired temporary Vercel URL:

`https://levonwb-partner.vercel.app`

Future planned domain:

`https://levonwb-partner.pro`

### New Deployment Files

Added:

- `package.json` — project metadata and scripts.
- `server.js` — simple local static server.
- `vercel.json` — Vercel configuration.
- `DEPLOY.md` — simple deployment guide in Russian.
- `robots.txt` — robots file with sitemap link.
- `sitemap.xml` — sitemap for the temporary Vercel address.
- `assets/favicon.svg` — favicon.
- `assets/og-preview.jpg` — social preview image.
- `assets/wb-sales-hero-optimized.jpg` — optimized hero background.
- `assets/levon-portrait-optimized.jpg` — optimized portrait image.

### Current Deployment Scripts

`package.json`:

```json
{
  "name": "levonwb-partner",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node -e \"console.log('Static landing is ready for Vercel')\"",
    "check": "node --check script.js"
  }
}
```

The site is static. Vercel can publish it without a framework build.

### SEO Added

Current title:

`Операционный партнёр Wildberries | Рост продаж и управление проектом`

Current meta description:

`Помогаю производителям, инвесторам и владельцам бизнеса масштабировать продажи на Wildberries через аналитику, рекламу, экономику, карточки и гипотезы роста.`

Added:

- canonical URL for `https://levonwb-partner.vercel.app/`;
- Open Graph title;
- Open Graph description;
- Open Graph URL;
- Open Graph image;
- Open Graph image width/height;
- Twitter card;
- Twitter title;
- Twitter description;
- Twitter image;
- favicon;
- apple touch icon;
- robots meta tag;
- theme color.

Important future task:

When `levonwb-partner.pro` is connected, replace `https://levonwb-partner.vercel.app/` with `https://levonwb-partner.pro/` in:

- `index.html`;
- `robots.txt`;
- `sitemap.xml`;
- `DEPLOY.md`, if desired.

### Analytics And Advertising Placeholders

In `index.html`, comments were added in the `<head>`:

```html
<!-- ВСТАВИТЬ ID ЯНДЕКС.МЕТРИКИ: добавьте код счетчика Яндекс.Метрики ниже перед запуском рекламы. -->
<!-- ВСТАВИТЬ ID GOOGLE ANALYTICS: добавьте gtag-код Google Analytics ниже при необходимости. -->
<!-- ВСТАВИТЬ РЕКЛАМНЫЙ ПИКСЕЛЬ: добавьте пиксели рекламных систем ниже при подключении площадок. -->
```

UTM logic was added to `script.js`:

- captures `utm_source`;
- captures `utm_medium`;
- captures `utm_campaign`;
- captures `utm_content`;
- captures `utm_term`;
- stores them in `sessionStorage` under `levonwb_utm`.

This prepares the site for future CRM/form/analytics integration.

### CTA Changes For Ads

All main CTA links now point to Telegram:

- `Обсудить партнёрство` -> `https://t.me/levon_stepanian`
- `Написать в Telegram` -> `https://t.me/levon_stepanian`
- `Связаться` -> `https://t.me/levon_stepanian`
- `Оставить заявку` -> `https://t.me/levon_stepanian`

The previous secondary hero button `Смотреть показатели` was replaced with `Написать в Telegram`.

The contact form button was changed from a mail submit action to a Telegram CTA:

- current button: `Оставить заявку`
- link: `https://t.me/levon_stepanian`

The form note now says:

`Кнопка откроет Telegram, чтобы обсудить проект напрямую.`

The form element still keeps a `mailto:` action as fallback, but the visible action is Telegram.

### Current Contact Links

Telegram:

- `@levon_stepanian`
- `https://t.me/levon_stepanian`

Email:

- `stepanianlevon022@mail.ru`
- `mailto:stepanianlevon022@mail.ru`

Phone:

- `+7 (928) 322-24-48`
- `tel:+79283222448`

### Image Optimization

New optimized files:

- `assets/levon-portrait-optimized.jpg`: JPEG, 900 x 1200, about 307 KB.
- `assets/wb-sales-hero-optimized.jpg`: JPEG, 1600 x 900, about 232 KB.
- `assets/og-preview.jpg`: JPEG, 1200 x 675, about 151 KB.

Current HTML uses:

- hero background: `assets/wb-sales-hero-optimized.jpg`;
- portrait photo: `assets/levon-portrait-optimized.jpg`;
- social preview: `assets/og-preview.jpg`.

Original larger files currently remain in `assets/` as source/reference files:

- `assets/wb-sales-hero.png`;
- `assets/levon-portrait.png`.

They are not referenced by the current HTML.

### Verification After Launch Prep

Local machine limitations:

- `npm` command is not available locally.
- `vercel` CLI is not installed locally.
- Because of that, `npm run check` and `npm run build` could not be executed through npm on this machine.
- Equivalent checks were run directly through `node`.

Checks passed:

- `node --check script.js`;
- `package.json` is valid JSON;
- `vercel.json` is valid JSON;
- direct build command prints `Static landing is ready for Vercel`;
- optimized images are valid JPEGs;
- favicon is valid SVG;
- browser page load via `http://127.0.0.1:8080/` had no console errors;
- title and meta description are present;
- OG image is present;
- favicon link is present;
- contact links are correct;
- all main CTA texts link to Telegram;
- lightbox markup exists;
- active result slide has 5 screenshots;
- mobile viewport check at 390 x 844 showed no horizontal overflow.

Mobile check result:

- viewport width: 390;
- document width: 390;
- horizontal overflow: false;
- mobile nav is hidden;
- portrait card fits within viewport.

### Current Launch Status

The project is ready to be published as a static landing page.

Not yet done:

- actual Vercel deployment from this machine, because Vercel CLI is not installed and Vercel account authorization is required;
- real Yandex Metrica ID insertion;
- real Google Analytics ID insertion;
- real advertising pixel insertion;
- purchase/connection of `levonwb-partner.pro`.

Recommended next step:

Deploy through the Vercel web interface or install/authenticate Vercel CLI, then publish the project as `levonwb-partner`.

### Final Technical Audit Before Publication - 2026-05-20

User asked for a final technical check before publication. The site was checked through the local server at `http://127.0.0.1:8080/`.

Checks completed:

- browser console errors: none found;
- CTA links checked:
  - `Обсудить партнёрство` -> `https://t.me/levon_stepanian`, opens in a new tab;
  - `Написать в Telegram` -> `https://t.me/levon_stepanian`, opens in a new tab;
  - `Связаться` -> `https://t.me/levon_stepanian`, opens in a new tab;
  - `Оставить заявку` -> `https://t.me/levon_stepanian`, opens in a new tab;
- contact links checked:
  - Telegram -> `https://t.me/levon_stepanian`;
  - email -> `mailto:stepanianlevon022@mail.ru`;
  - phone -> `tel:+79283222448`;
- result lightbox checked:
  - active slide has 5 screenshots;
  - click on screenshot opens modal;
  - original screenshot opens in large size;
  - month label appears, for example `Ноябрь — БЫЛО`;
  - metric label appears, for example `Заказы`;
  - next arrow switches from November to December;
  - close button closes modal;
- responsive checks completed at:
  - 320 x 844;
  - 390 x 844;
  - 768 x 1024;
  - 1024 x 900;
- no horizontal page scroll found on those widths;
- no broken image paths found in browser;
- no missing local asset references found in `index.html`;
- no clipped visible text found by automated check outside the intentional results slider;
- local measured page load through the static server was about 226 ms on the desktop test viewport;
- `node --check script.js` passed;
- `package.json` and `vercel.json` are valid JSON.

Important nuance from the audit:

- the results slider intentionally keeps inactive slides outside the visible area inside `.case-track`; this is not page-level horizontal scroll and does not create body/document overflow.

Remaining non-critical notes:

- `npm` is not installed on the current machine, so npm scripts were not executed through `npm`;
- `vercel` CLI is not installed on the current machine, so a real CLI deployment was not executed;
- actual analytics IDs are still placeholders and must be inserted before paid traffic if tracking is required;
- original large source files remain in `assets/` as unused references: `wb-sales-hero.png` and `levon-portrait.png`.

### Working Telegram Contact Form - 2026-05-28

User requested that the visual contact form become a real working lead form and send applications to Telegram.

Implemented files:

- `index.html`
  - contact form now posts to `/api/telegram`;
  - fields `Имя`, `Контакт`, `Проект` are required;
  - visible action is now a real submit button: `Оставить заявку`;
  - reserve Telegram link remains in the form note;
  - form has live status output through `data-form-status`.
- `script.js`
  - intercepts form submit;
  - sends JSON through `fetch()` to `/api/telegram`;
  - includes `name`, `contact`, `message`, current page URL, and saved UTM tags;
  - disables the submit button while sending;
  - on success shows: `Спасибо! Заявка отправлена, я скоро свяжусь с вами.`;
  - on error shows: `Не удалось отправить заявку. Напишите мне напрямую в Telegram.`;
  - after successful send calls Yandex Metrica goal:
    `ym(109329838, "reachGoal", "form_submit")`;
  - keeps all direct Telegram buttons as backup.
- `api/telegram.js`
  - new Vercel-compatible serverless endpoint;
  - accepts only `POST`;
  - reads environment variables:
    - `TELEGRAM_BOT_TOKEN`;
    - `TELEGRAM_CHAT_ID`;
  - does not store Telegram token in public code;
  - validates required fields;
  - sends formatted message to Telegram through `https://api.telegram.org/bot<TOKEN>/sendMessage`;
  - returns JSON success/error responses.
- `server.js`
  - local development server now routes `/api/telegram` to the same handler for local checks.
- `styles.css`
  - added form status styling;
  - disabled submit button styling;
  - form note Telegram link styling.
- `DEPLOY.md`
  - updated with instructions for creating Telegram bot through `@BotFather`;
  - explains where to get `TELEGRAM_BOT_TOKEN`;
  - explains how to get `TELEGRAM_CHAT_ID` through `getUpdates`;
  - explains where to add environment variables in Vercel:
    `Project -> Settings -> Environment Variables`;
  - explains that the project must be redeployed after adding variables.

Important environment setup:

- On Vercel, add:
  - `TELEGRAM_BOT_TOKEN = token from BotFather`;
  - `TELEGRAM_CHAT_ID = personal or group chat id`.
- Without these variables, the form intentionally returns an error, and the user sees the Telegram fallback message.

Security decision:

- Telegram bot token is not stored in `index.html`, `script.js`, or any public frontend code.
- Token must only live in Vercel Environment Variables.

Testing expectations:

- Local test without Telegram variables should show the error message.
- Full successful Telegram delivery requires real `TELEGRAM_BOT_TOKEN` and `TELEGRAM_CHAT_ID`.

### Contact Channels Update - 2026-05-29

User requested additional contact methods and CTA cleanup.

Implemented:

- Header CTA remains `Обсудить партнёрство`.
- Header CTA is now the main red gradient CTA with premium hover animation and adaptive sizing.
- Hero block:
  - removed lower duplicate `Обсудить партнёрство` button;
  - kept only the main button `Написать в Telegram`;
  - added compact line: `Также можно связаться через:`;
  - added quick contact buttons:
    - `Telegram` -> `https://t.me/levon_stepanian`;
    - `WhatsApp` -> `https://wa.me/79283222448`;
    - `MAX` -> `https://max.ru/u/f9LHodD0cOImftilzkCl4kAeStuy19clTRT-OfiR8N3evEhlv8ZSIie6VrY`.
- Contact section:
  - existing contact form remains working through `/api/telegram`;
  - after the form, added quick contact buttons:
    - `Telegram`;
    - `WhatsApp`;
    - `MAX`;
  - direct Telegram fallback remains in form note.
- Yandex Metrica click goals:
  - Telegram links use `telegram_click`;
  - WhatsApp links use `whatsapp_click`;
  - MAX links use `max_click`;
  - implementation uses guarded inline handlers such as:
    `window.ym&&ym(109329838,'reachGoal','telegram_click')`
    so links still open even if Yandex Metrica has not loaded yet.

Design decisions:

- Quick contact buttons use compact pill styling.
- Hero quick links use dark glass/premium style.
- Form quick links use light premium style matching the contact card.
- All quick links open in a new tab on desktop through `target="_blank"` and use regular HTTPS mobile deep links for app handoff on iPhone/Android.

Verification:

- `node --check script.js` passed.
- `node --check server.js` passed.
- `node --check api/telegram.js` passed.
- `package.json` and `vercel.json` are valid JSON.
- Direct Vercel build command prints `Static landing is ready for Vercel`.
- Browser check at 320, 390, 768, and 1280 widths found no horizontal overflow.
- Console errors: none found in browser check.
- Header CTA text and gradient style verified.
- Hero contains only one main button: `Написать в Telegram`.
- Hero quick buttons count: 3.
- Form quick buttons count: 3.

### Telegram UTM Notification Update - 2026-05-29

User requested that all visitor UTM tags be saved and sent to Telegram in a readable format.

Existing frontend behavior:

- `script.js` captures UTM tags from the landing page URL;
- stores them in `sessionStorage` under `levonwb_utm`;
- passes saved UTM tags with the form payload to `/api/telegram`.

Updated backend behavior in `api/telegram.js`:

- Telegram notification now includes:
  - `Источник: <utm_source or не передан>`;
  - `Канал: <utm_medium or не передан>`;
  - `Кампания: <utm_campaign or не передан>`;
  - `Контент: <utm_content or не передан>`.
- Example URL:
  `?utm_source=instagram&utm_medium=bio`
- Example Telegram output:
  - `Источник: instagram`;
  - `Канал: bio`;
  - `Кампания: не передан`;
  - `Контент: не передан`.

Verification:

- `api/telegram.js` test with mocked Telegram fetch returned `200 {"ok":true}`;
- generated Telegram text contained `Источник: instagram` and `Канал: bio`;
- missing `utm_campaign` and `utm_content` appeared as `не передан`;
- `node --check api/telegram.js` passed;
- `node --check script.js` passed;
- direct Vercel build command prints `Static landing is ready for Vercel`.

### Final GitHub ZIP Update - 2026-05-29

User requested one final ZIP for uploading the project to GitHub, with these changes:

- add Instagram next to Telegram / WhatsApp / MAX on the hero screen;
- add Instagram to the lower contact block after the form;
- Instagram link corrected by the user to:
  `https://www.instagram.com/spartanec_wb?igsh=ZTFmcDhta284ZXRy&utm_source=qr`;
- add a trust block under `Написать в Telegram` and above quick contacts:
  - `5+ лет` / `на Wildberries`;
  - `90+ млн ₽` / `заказов за 3 месяца в одном из проектов`;
  - `От аналитики` / `до масштабирования продаж`;
- update Telegram notification format:
  - `🔥 Новая заявка`;
  - `👤 Имя`;
  - `📱 Контакт`;
  - `📦 Проект`;
  - `📍 Источник`;
  - readable UTM fields: `Источник`, `Канал`, `Кампания`, `Контент`.

Files changed for this update:

- `index.html`;
- `styles.css`;
- `api/telegram.js`;
- `PROJECT_SUMMARY.md`.

Verification for this update:

- Instagram appears in hero quick contacts and form quick contacts;
- Instagram href uses the corrected `spartanec_wb` URL;
- hero still contains one main CTA: `Написать в Telegram`;
- trust block contains 3 cards;
- browser check at 390 and 1280 widths found no horizontal overflow;
- browser console errors: none found during check;
- Telegram notification mock returned `200 {"ok":true}` with the new readable format.

### Strategic Landing Rebuild - 2026-05-30

User changed the sales strategy of the landing page.

New main idea:

- not “looking for partners”;
- now the core offer is helping Wildberries sellers find profit growth points, remove money leaks, and implement changes until a measurable result.

Partnership remains, but is now a separate premium block lower on the page, not the main hero offer.

Implemented in `index.html`:

- Hero headline changed to:
  `Помогаю селлерам Wildberries находить точки роста прибыли и масштабировать бизнес`
- Hero subheading changed to:
  `Анализирую экономику, рекламу, логистику и процессы магазина. Не просто нахожу проблемы — помогаю внедрить изменения и довести их до результата.`
- Hero primary CTA:
  `Получить аудит магазина` -> `#contact`
- Hero secondary CTA:
  `Написать в Telegram`
- Hero quick contacts remain:
  - Telegram;
  - WhatsApp;
  - MAX;
  - Instagram.
- Header nav now scrolls to one-page sections:
  - `Аудит`;
  - `Этапы`;
  - `Результаты`;
  - `Партнерство`;
  - `Контакты`.
- Header CTA changed to:
  `Получить аудит`.
- Hero trust cards updated:
  - `5+ лет` / `на Wildberries`;
  - `90+ млн ₽` / `заказов за 3 месяца в одном из проектов`;
  - `От аналитики` / `до внедрения и роста прибыли`.
- Added pain block:
  - `Оборот растёт, а чистой прибыли почти нет`;
  - `Не понимаю, куда уходят деньги`;
  - `Реклама работает, но маржа падает`;
  - `Логистика и упаковка съедают прибыль`;
  - `Есть продажи, но нет системы роста`;
  - `Боюсь запускать новый товар и потерять деньги`.
- Rebuilt services block around the main offer:
  `Аудит и увеличение прибыли магазина Wildberries`.
- Audit path:
  `Диагностика → Поиск потерь → План действий → Внедрение → Контроль результата`.
- Audit checklist:
  - economy;
  - ads and DRR;
  - logistics;
  - packaging and dimensions;
  - taxes and expenses;
  - buyout rate;
  - cards and conversion;
  - stock and warehouses.
- Added additional directions:
  - `Сопровождение после аудита`;
  - `Запуск магазина с нуля`;
  - `Практическое обучение и наставничество`.
- Results section kept existing screenshots/lightbox and current case logic.
- Results copy changed so the case reads as complex work:
  - sales growth;
  - expense optimization;
  - economic improvement;
  - net profit growth.
- Added placeholder for second case:
  `От запуска до 6,5 млн ₽ за месяц`
  with text:
  `Кейс в подготовке — добавлю после закрытия месяца.`
- Added work process block:
  - `Знакомство`;
  - `Аудит`;
  - `План действий`;
  - `Внедрение`;
  - `Контроль результата`.
- Added premium partnership block:
  `Рассматриваю ограниченное количество проектов для долгосрочного партнёрства`.
- Partnership wording avoids phrase `сильный товар`.
- Uses approved wording:
  `товарная линейка с потенциалом масштабирования`.
- Contact section updated:
  - visual email removed;
  - contact links now show Telegram, WhatsApp, MAX, Instagram, phone;
  - form still sends through `/api/telegram`.
- Form now includes required select:
  `Что вас интересует?`
  with options:
  - `Аудит магазина`;
  - `Увеличение прибыли и сопровождение`;
  - `Запуск магазина с нуля`;
  - `Обучение / наставничество`;
  - `Партнёрство`;
  - `Другое`.

Implemented in `script.js`:

- form payload now includes `interest`;
- validation requires interest, name, contact, and project description;
- existing UTM capture remains unchanged;
- `form_submit` Yandex Metrica goal remains on successful form submission.

Implemented in `api/telegram.js`:

- Telegram notification format changed to:
  - `🔥 Новая заявка с сайта`;
  - `🎯 Интерес:`;
  - `👤 Имя:`;
  - `📱 Контакт:`;
  - `📦 Проект:`;
  - `🌐 Страница:`;
  - `📍 Источник:`;
  - readable UTM values:
    - `Источник`;
    - `Канал`;
    - `Кампания`;
    - `Контент`;
  - missing UTM values show `не передан`.
- Environment variable names remain unchanged:
  - `TELEGRAM_BOT_TOKEN`;
  - `TELEGRAM_CHAT_ID`.

Implemented in `styles.css`:

- dark premium pain cards;
- audit offer panel;
- audit flow and checklist chips;
- responsive direction cards;
- five-step workflow;
- premium partnership block;
- select input styling;
- case placeholder styling;
- mobile adaptations for 390px and desktop.

Verification:

- `node --check script.js` passed;
- `node --check api/telegram.js` passed;
- `node --check server.js` passed;
- mocked Telegram request returned `200 {"ok":true}` with the new notification format;
- browser check at 390px showed no horizontal overflow;
- browser check at 1280px showed no horizontal overflow;
- console errors: none found during browser checks;
- Vercel build command prints `Static landing is ready for Vercel`.

Files changed:

- `index.html`;
- `styles.css`;
- `script.js`;
- `api/telegram.js`;
- `PROJECT_SUMMARY.md`.
